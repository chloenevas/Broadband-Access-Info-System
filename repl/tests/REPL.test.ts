import "@testing-library/jest-dom";

describe("Math Operations", () => {
  test("Addition", () => {
    expect(2 + 2).toBe(4);
  });

  test("Subtraction", () => {
    expect(5 - 3).toBe(2);
  });
});
